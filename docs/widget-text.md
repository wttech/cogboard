# [Home](/cogboard/) >> Text widget

#### Configuration:
* `Text` - any text
* `Text Size` - text sizes from `XS(h6)` to `XXL(h1)` () >> default: `M(h4)`
* `Vertical Text` - rotate text by 90 degrees >> default: `false`